

1. create table of all students (fsuID, first, last, email, teamID[init null])
2. for each student, if student has no teamID:
    1. create a team (teamID, teamname, member IDs, domjudge user/pass)
    2. assign domjudge login to the team
    3. check original csv for team member FSUIDs
        1. if student does not exist yet, create empty student entry
        2. assign students with those FSUIDs to the team

ON THURSDAY AFTERNOON:

1. export team data from google forms
2. generate flask database from form data
3. export team data w/ passwords to csv
4. upload team data to domjudge

4. test email system
5. test credentials with login
