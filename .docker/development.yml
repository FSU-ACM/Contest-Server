version: '2'
services:
  db:
    extends:
      file: docker-compose.yml
      service: db
    ports:
      - "8027:27017"
    volumes:
      - ../.data:/data/db
  webapp:
    extends:
      file: docker-compose.yml
      service: webapp
    volumes:
      - ..:/webapp
    ports:
      - "8080:80"
    environment:
      FLASK_CONFIG: '/webapp/config/development.py'
